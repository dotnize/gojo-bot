{
  "name": "gojo-bot",
  "version": "1.0.0",
  "private": true,
  "description": "A Discord bot.",
  "license": "MIT",
  "author": "Nathaniel John Tampus <nat@nize.ph>",
  "type": "module",
  "imports": {
    "#/*": "./src/*"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.ts",
    "dev": "node --env-file-if-exists=.env --watch src/index.ts",
    "deploy-cmds": "node --env-file-if-exists=.env src/deploy-commands.ts",
    "prepare": "husky",
    "lint": "oxlint --type-aware --type-check",
    "lint:fix": "oxlint --type-aware --type-check --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "pnpm format && pnpm lint",
    "deps": "pnpm dlx taze@latest -Ilw --maturity-period 3",
    "intent": "pnpm dlx @tanstack/intent@latest"
  },
  "dependencies": {
    "@tanstack/ai": "0.57.0",
    "@tanstack/ai-gemini": "0.31.1",
    "discord.js": "14.27.0"
  },
  "devDependencies": {
    "@types/node": "26.6.2",
    "husky": "9.1.7",
    "lint-staged": "17.5.1",
    "oxfmt": "0.68.0",
    "oxlint": "1.83.0",
    "oxlint-tsgolint": "7.0.2002",
    "typescript": "7.0.2"
  },
  "lint-staged": {
    "*": "oxfmt --no-error-on-unmatched-pattern"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@12.5.1",
  "intent": {
    "skills": [
      "@tanstack/*"
    ]
  }
}
